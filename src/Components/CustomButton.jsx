import { Button } from "react-bootstrap";

const CustomButton = () => {
  return (
    <div>
      <Button className="text-danger"variant="primary">Click Me</Button>
    </div>
  );
}

export default CustomButton;
